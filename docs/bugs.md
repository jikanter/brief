[x] - Brief drops inline markdown everywhere, not only in soft sections: the body parser rebuilt text from a whitelist of pulldown-cmark events (`src/parse/body.rs`), so any unhandled event vanished silently. Four confirmed symptoms: link URLs dropped (label kept) in the goal, every constraint tier and the deliverable; `**bold**` / `_em_` markers dropped; inline code dropped *entirely* from H1 and H2 headings (an H1 reading ``# Add `brief emit xml` to the tool`` parsed as the goal `Add  to the tool`) because there was an `Event::Code if in_item` arm but none for `in_heading`; deliverable paragraphs concatenated with no separator. Fixed: headings, list items and paragraphs collect through one inline collector, and an inline container (a link, emphasis, an image) is taken whole from the source text rather than rebuilt from its events, so parsed text is the author's source markdown for that span.
[ ] - Brief still does not emit valid markdown (no spaces between headings and body). 
[x] - `brief check` flags non-sacred files: `flat/**` marks `flatter/x.md` sacred. `src/check.rs` falls back to a string `starts_with` on the pattern with `/**` trimmed, so `src/auth/**` also blocks `src/authz/` and `src/authentication/`. Reaches the sacred-region hooks, which then refuse legitimate edits. Fixed: matching moved to `src/pathmatch.rs`, which compares path components via `glob::Pattern` with `require_literal_separator` and never touches the filesystem.
[x] - `brief validate` warns "Sacred path `dir/**` matches no files" when `dir` holds files but no subdirectories. `glob::glob` yields nothing for a trailing `**` over a flat directory. Fixed: both the sacred check and the constraint-scope check now ask `pathmatch::pattern_matches_any_file`, which walks the pattern's literal root and answers with the same matcher `brief check` uses. `directory_prefix_exists` is gone; a directory holding only directories (`empty/**`) now warns, where the scope check used to stay silent.
[x] - `brief emit aider --install` drops comments from a user's `.aider.conf.yml`. `merge_aider_conf` deserialized the file and re-emitted it, so a leading `# comment` and an inline `read: other.md # keep` were both lost, along with the author's key order. Broke the non-destructive-install hard constraint. Fixed: the merge is now a line splice that edits only the `read` entry and an added `model` line. A shape it cannot edit safely (an anchor or alias on `read`, more than one document, tabs for indentation, `read` nested under another key) is an error naming the file, and nothing is written.
[x] - `brief skill emit` wrote the goal into `description:` unquoted, so a goal carrying a colon -- which an English sentence usually does -- produced a SKILL.md whose own frontmatter would not parse, and the command then failed its own validation with `mapping values are not allowed in this context`. Cursor had had a `yaml_scalar` quoter since it shipped; the skill emitter never used it. Found while running `bugfix-sacred-glob-aider.brief.md` through `brief skill emit`. Fixed: the quoter moved to `src/emit/yaml.rs` and both emitters share it.
